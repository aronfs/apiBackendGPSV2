meta {
  name: Refrescar Token
  type: http
  seq: 5
}

get {
  url: {{url_backend}}/api/v1/accounts/refreshToken
  body: none
  auth: bearer
}

auth:bearer {
  token: {{Authorization}}
}

settings {
  encodeUrl: true
  timeout: 0
}
