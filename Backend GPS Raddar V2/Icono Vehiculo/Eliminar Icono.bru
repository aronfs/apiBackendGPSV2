meta {
  name: Eliminar Icono
  type: http
  seq: 4
}

delete {
  url: {{url_backend}}/api/v1/icon/deleteIcon?deviceId=32
  body: none
  auth: bearer
}

params:query {
  deviceId: 32
}

auth:bearer {
  token: {{Authorization}}
}

settings {
  encodeUrl: true
  timeout: 0
}
