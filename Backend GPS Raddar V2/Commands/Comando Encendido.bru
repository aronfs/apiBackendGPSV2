meta {
  name: Comando Encendido
  type: http
  seq: 2
}

patch {
  url: {{url_backend}}/api/v1/commands/engineStart?deviceId=36
  body: none
  auth: bearer
}

params:query {
  deviceId: 36
}

auth:bearer {
  token: {{Authorization}}
}

settings {
  encodeUrl: true
  timeout: 0
}
