meta {
  name: Obtener Comandos
  type: http
  seq: 5
}

get {
  url: {{url_backend}}/api/v1/commands/getCommands?deviceId=32
  body: none
  auth: bearer
}

params:query {
  deviceId: 32
}

auth:bearer {
  token: {{Authorization}}
}

settings {
  encodeUrl: true
  timeout: 0
}
