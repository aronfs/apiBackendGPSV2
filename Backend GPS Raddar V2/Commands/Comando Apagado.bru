meta {
  name: Comando Apagado
  type: http
  seq: 3
}

patch {
  url: {{url_backend}}/api/v1/commands/engineStop?deviceId=36
  body: none
  auth: bearer
}

params:query {
  deviceId: 36
}

auth:bearer {
  token: {{Authorization}}
}

settings {
  encodeUrl: true
  timeout: 0
}
